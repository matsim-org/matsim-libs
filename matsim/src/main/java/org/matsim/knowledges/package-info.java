/**
 * Knowledge is a concept used in some scenarios where agents have a choice in where to conduct a certain activity.
 * It is not backed by any MATSim functionality. It is only here as a data container because the (superceded) PopulationV4 data
 * format specifies it. Do not use it for new code. You can attach custom data to Persons using ObjectAttributes.
 */
package org.matsim.knowledges;