/**
 * Package defining data structures to handle cliques for joint optimisation.
 * The main idea behind this package is that cliques are the individuals of a 
 * "population of cliques". Thus, those data structures can be handled by the
 * default MATSim classes (with some extra care needed), and passed to custom
 * strategy modules.
 */
package playground.thibautd.jointtripsoptimizer.population;
